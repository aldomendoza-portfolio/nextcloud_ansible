all:
  children:
    nextcloud_servers:
      hosts:
        server1:
          ansible_host: 74.208.32.232
          # Inicialmente nos conectamos como root
          # Una vez que el rol 'system' termine, podrías necesitar cambiar 'ansible_user' a 'amendoza' para futuras ejecuciones.
          ansible_user: amendoza
          ansible_ssh_private_key_file: "./ssh_keys/amendoza_ed25519"
          # Descomenta la siguiente línea y pon tu contraseña si te conectas por contraseña
          # ansible_password: "[PASSWORD]"
